# 客戶交付版說明

本資料夾為精簡交付包，**可瀏覽／展示官網前端與靜態內容**。

## 有什麼
- Next.js 前端介面、頁面、樣式、靜態素材（public）
- 基本設定檔，可正常 `npm install` / `npm run dev` / `npm run build`

## 沒有什麼（維運／進階整合另議）
- `.env` 金鑰、Webhook Secret
- `.git` 完整開發歷史
- WordPress Code Snippets 原始碼
- SEO／GEO／JSON-LD 完整策略
- Headless CMS 即時串接層（系列／首頁區塊／Woo 完整 API）
- 綠界／登入／Supabase 金流與驗證整合
- `scripts/` 維運腳本

## 怎麼用
1. 複製 `.env.example` 為 `.env.local`（僅放客戶環境需要的公開變數）
2. `npm install`
3. `npm run dev` 或 `npm run build`

內容更新請使用已上線的 WordPress 後台（編輯權）。  
進階快取刷新、SEO、金流串接請洽原開發／維護合約。
